# AI Background Remover Web App

This is a full-stack web application that removes the background from images using AI.  
The backend is built with **FastAPI** and **Rembg**, while the frontend uses **React + TypeScript**.  
Users can upload an image, and the app will return a PNG image with a transparent background.
